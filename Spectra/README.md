# Spectra iOS App

This directory contains the SwiftUI source layout for the iOS app.

## Create the Xcode project
1. Open Xcode.
2. Create a new iOS App project named "Spectra".
3. Set the project location to this `Spectra/` directory.
4. Ensure the folder groups match the structure in this directory.
